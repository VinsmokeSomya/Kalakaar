{
  "version": 2,
  "buildCommand": "cd kalakaar && npm run build",
  "outputDirectory": "kalakaar/.svelte-kit/output",
  "installCommand": "npm ci && cd kalakaar && npm ci",
  "framework": "sveltekit",
  "regions": ["iad1"],
  "env": {
    "GEMINI_API_KEY": "${GEMINI_API_KEY}"
  }
} 